import React from 'react';\n\ninterface ResearchAgentProps {\n  onTaskComplete: (result: string) => void;\n}\n\nconst ResearchAgent: React.FC<ResearchAgentProps> = ({ onTaskComplete }) => {\n  // Research Agent for web search and summarization\n  // Uses LLM backend (Anthropic, OpenAI, Google)\n  // Participates in multi-round discussions\n  // Applies specialized knowledge in research domains\n  \n  return (\n    <div className="agent-card">\n      <h3>Research Agent</h3>\n      <p>Web search, data synthesis, and summarization specialist</p>\n    </div>\n  );\n};\n\nexport default ResearchAgent;
